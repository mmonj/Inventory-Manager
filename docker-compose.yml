version: '3'
services:
  app:
    build:
      context: .
      args:
        BUILD_MODE: production
    image: inventory-manager-django
    container_name: inventory-manager
    ports:
      - 8001:8000
    volumes:
      - ./:/app
    env_file:
      - .env
