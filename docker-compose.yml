version: '3'
services:
  app:
    build: .
    container_name: inventory-manager
    ports:
      - 8001:8000
    volumes:
      - ./inventory_manager:/app
    environment:
      - TZ=America/New_York
